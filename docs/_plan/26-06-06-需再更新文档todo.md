下方已列出需要更新的文档列表。
然后派出子Agent使用`/doc-format`SKILL整改这些文档，严格使用这样的提示词`使用 /doc-format 整改 `docs/api/xxxx.md`文档是否符合文档规范，缺少代码示例时搜索源码补全。`
然后每个文档整改完都在todo列表上确定。

- [ ] docs\api\advisors.md 应该让AdviceManager占据主要的位置，最上方。调试方面的API可以放在后面
- [ ] docs\api\autoplay.md 重写
- [ ] docs\api\component.md 弹窗<API>里很多都没示例，引用也怪怪的
- [ ] docs\api\culture.md 有点混乱，重写。没有解释`player`的由来，很容易和`Players`混淆
- [ ] docs\api\cities.md 重新设计。实例获取，属性，方法。
- [ ] docs\api\configuration.md 写有很多未验证的，但我手动搜索了都是有的。需要补充。
- [ ] docs\api\audio-debug.md 专注音频API，拆分debug出去。重命名为`audio.md`
- [ ] docs\api\debug.md 新增文档，专注Debug调试。`UI.Debug`是其一。
- [ ] docs\api\game-effects.md 需求Requirement是什么？没讲清楚，也没实例代码。
- [ ] docs\api\globals.md 有什么用？是否要删掉？
---
- [ ] docs\api\notifications.md 缺少`Game.Notifications.send`
- [ ] docs\api\network.md 需要整理
- [ ] docs\api\religion.md 补充`city.Religion`等等内容
- [ ] docs\api\reflection.md 需要整理补充
- [ ] docs\api\ui-objects.md 需要整理补充。把`component.md`合并进来？
- [ ] docs\api\units.md 更新整理补充。
- [ ] docs\api\social.md 需要补充<API>弹窗。
- [ ] docs\api\online.md 新增文档，放在全局对象类里。`Online.`
- [ ] docs\api\resources.md 更新整理补充。系统级
- [ ] docs\api\combat.md 新增战斗系统文档，`Game.Combat`、`unit.Combat`、`player.Armies`、`player.Formations`

------
- [ ] docs\api\globals.md 里面出现了很多未验证方法，但我去搜都是有源码的。想向你确认下具体的检索方式，以及出现检索无果的原因。麻烦如实说明检索失败的缘由，并将本次问题复盘总结。最后提炼最关键补充至SKILL文档路径：`.agents\skills\doc-format\SKILL.md`中留存经验。
举个例子：`GameContext.localObserverID` 可在路径`D:\Games Design\Civ7_mod\.官方变动\modules\base-standard\ui\root-game.js`的第131行找到。
SKILL：`/doc-format`

使用 /doc-format 整改以下文档是否符合文档规范，缺少代码示例时搜索源码补全。
- [ ] docs\api\component.md 在主范围中添加了`ComponentRoot`和`ComponentID`。
- [ ] docs\api\ui-objects.md 在主范围中添加了`InterfaceMode`，并且调整了其他顺序。

------
把 SKILL：`.agents\skills\doc-format` 更新成 /doc-update ，增加以下意图流程：
- 格式刷：检查文档格式，根据规范和范例决定更新文档格式。（不会对API查漏补缺）
- 补全：积极查找搜寻可能遗漏的API
- 验证：检查API是否正确，积极校验
- 维护网站：不动文档，对网站网页相关进行检查，比如侧边栏是不是都包含全部文档了；主页更新日期；主页展示改动；

分析这些改动是否合适？意图是否会冲突重复？那些环节需要修改？
从简到繁杂流程排序这些意图流程，以及意图名称是否要换？

意图流程还要保留SKILL原有的，把它们加起来一起重新思考方案。
并且再加一个意图流程：
- 维护网站：不动文档，对网站网页相关进行检查，比如侧边栏是不是都包含全部文档了；主页更新日期；主页展示改动；


------

我梳理汇总情况如下，麻烦帮忙优化整合至SKILL`doc-format `里，做到精简规范。
`.agents\skills\doc-format\SKILL.md`
`doc_type`分类决策树：
- `object`（从object-api改名）
    - 展示一个全局对象的方法和属性？
    - 简单型：只有 Manager（如 Camera）
        - 特征：单一对象。
    - 标准型：Manager + Entity（如 Cities + city）
    - 复杂型：Manager + Entity + 子系统需拆分（如 Players）
        - 特征：有明确的全局入口对象，有 `get()` 获取实例，实例上有属性和子系统。
- `system`（从system-topic改名）
    - 解释一个游戏系统，涉及多个对象？
    - 比如：外交系统，科技文化。
    - 特征：跨多个对象的游戏系统。
- `data`（从reference改名）
    - 纯数据/事件列表/常量/枚举？
    - 比如：`events`、`game-info`等等。
    - 特征：静态参考。
- `other`（从ui-api改名）
    - 和游戏本体关系较低？
    - 比如： （UI、本地化、音频、调试、网络、社交、手柄输入）
    - 比如：`Component`、`Audio`、`Locale`等等。
    - 特征：非游戏内系统。
- `generated`（从generated-reference改名）
    - 脚本自动生成？
    - 比如：`constants`等等。
    - 特征：不可手动编辑，由脚本生成。
