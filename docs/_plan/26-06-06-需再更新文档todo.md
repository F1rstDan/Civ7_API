- [] docs\api\advisors.md 应该让AdviceManager占据主要的位置，最上方。调试方面的API可以放在后面
- [] docs\api\autoplay.md 重写
- [] docs\api\component.md 弹窗<API>里很多都没示例，引用也怪怪的
- [] docs\api\culture.md 有点混乱，重写。没有解释`player`的由来，很容易和`Players`混淆
- [] docs\api\cities.md 重新设计。实例获取，属性，方法。
- [] docs\api\configuration.md 写有很多未验证的，但我手动搜索了都是有的。需要补充。
- [] docs\api\audio-debug.md 专注音频API，拆分debug出去。重命名为`audio.md`
- [] docs\api\debug.md 新增文档，专注Debug调试。`UI.Debug`是其一。

`Players`


`Cities`和`city = Cities.get(cityId)`
会有很多`city`的属性和方法。这样的API文档要怎么设计？
`city`这种叫做数据类型？primary_scope: 要怎么填写呢？文档要怎么说明？

同理：`Players`和`player = Players.get(GameContext.localPlayerID);`

现在这种文档（大型文档？），doc_type: object-api，这种分类合理吗？
还有什么特征？


关于`doc_type`分类

- 这种叫什么分类？依然是object-api吗？
```
全局对象：管理器 (Manager) → 实例：实体 (Entity) → 获取实例
Cities         →  city          (Cities.get(id))
Players        →  player        (Players.get(id))
Districts      →  district      (Districts.get(id))
Constructibles →  constructible (Constructibles.get(id))
Units          →  unit          (Units.get(id))
```
实例下还有属性和方法，子系统/组件 (Component)

- 还有关于游戏系统的，可能有自己的全局对象，也经常附属与`Game, Player, Units`等等。
比如：外交系统，科技文化。